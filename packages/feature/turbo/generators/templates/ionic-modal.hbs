  import {
    IonButton,
    IonButtons,
    IonContent,
    IonHeader,
    IonModal,
    IonTitle,
    IonToolbar,
  } from '@ionic/react';
  import { useState } from 'react';
  
  export const {{ pascalCase name}}Modal = () => {
    const [isOpen, setIsOpen] = useState(false);
  
    return (
      <>
        <IonButton onClick={() => setIsOpen(true)}>
          <strong>Edit</strong>
        </IonButton>
        <IonModal isOpen={isOpen}>
          <IonHeader>
            <IonToolbar>
              <IonTitle>{{ titleCase name}}</IonTitle>
              <IonButtons slot='end'>
                <IonButton onClick={() => setIsOpen(false)}>Close</IonButton>
              </IonButtons>
            </IonToolbar>
          </IonHeader>
          <IonContent>
           {{ titleCase name}}
          </IonContent>
        </IonModal>
      </>
    );
  };  